# 🖼️ Обработчик изображений

Автоматический скрипт для обработки изображений с добавлением текста, градиентов, треугольников и логотипов.

## ✨ Возможности

- 🔄 Автоматическое масштабирование изображений до 2160x2160 пикселей
- 📐 Создание двух версий: квадрат (2160x2160) и ландшафт (2310x1200)
- 📝 Добавление пользовательского текста с автоматическим переносом строк
- 🌈 Создание градиентов (нижний градиент и треугольник)
- 🏷️ Наложение логотипа в правом верхнем углу
- 🔲 Белая сетка поверх градиента с адаптивной прозрачностью
- 📊 Подробное логирование процесса обработки
- ⚡ Оптимизированная производительность с использованием numpy
- 🛡️ Надежная обработка ошибок и валидация входных данных

## 📋 Требования

- Python 3.7+
- Pillow (PIL) >= 9.0.0
- numpy >= 1.21.0

## 🚀 Установка

1. Клонируйте репозиторий или скачайте файлы
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## 📁 Структура папок

```
project/
├── main.py              # Основной скрипт
├── requirements.txt     # Зависимости
├── README.md           # Документация
├── img/                # Папка с исходными изображениями
├── logo/               # Папка с логотипом
├── fonts/              # Папка со шрифтами
└── output/             # Папка с результатами (создается автоматически)
```

## 🎯 Использование

1. Поместите изображения для обработки в папку `img/`
2. Поместите логотип в папку `logo/`
3. Поместите шрифт ACTAY-BOLD.OTF в папку `fonts/` (опционально)
4. Запустите скрипт:
```bash
python main.py
```
5. Введите текст для добавления на изображения
6. Дождитесь завершения обработки

**Результат:** Для каждого исходного изображения создается два файла:
- `{имя_файла}_square.png` - квадратное изображение 2160x2160
- `{имя_файла}_landscape.png` - ландшафтное изображение 2310x1200

## ⚙️ Конфигурация

Скрипт создает два изображения с разными настройками:

### 🟦 **Квадратное изображение (2160x2160)**
```python
CONFIG = {
    'target_size': 2160,                    # Размер выходного изображения
    'gradient_height_ratio': 0.4,          # Высота нижнего градиента (40% от высоты)
    'triangle_size': 600,                   # Размер треугольника
    'grid_square_size': 295,               # Размер квадрата сетки (в пикселях)
    'grid_line_thickness': 4,              # Толщина линий сетки (в пикселях)
    'grid_opacity_ratio': 0.3,             # Прозрачность сетки (30% от прозрачности градиента)
    'grid_vertical_offset': 75,            # Смещение сетки по вертикали вниз (в пикселях)
    'margins': {'text': 96, 'logo': 72},   # Отступы для текста и логотипа
    'font_size': 120,                       # Размер шрифта
    'line_spacing_ratio': 0.2,             # Межстрочный интервал
    'gradient_colors': {                    # Цвета градиентов
        'bottom': (70, 84, 154),           # #46549A - плотный цвет (0% прозрачности)
        'bottom_end': (42, 48, 80),        # #2A3050 - затухающий цвет (100% прозрачности)
        'triangle_start': (42, 48, 80),    # #2A3050 - начальный цвет треугольника
        'triangle_end': (70, 84, 154)      # #46549A - конечный цвет треугольника
    }
}
```

### 🟨 **Ландшафтное изображение (2310x1200)**
```python
CONFIG_LANDSCAPE = {
    'target_size': (2310, 1200),           # Ширина x Высота
    'gradient_height_ratio': 0.4,          # Высота нижнего градиента (40% от высоты)
    'triangle_size': 600,                   # Размер треугольника (как в квадратном)
    'grid_square_size': 200,               # Размер квадрата сетки (уменьшен)
    'grid_line_thickness': 3,              # Толщина линий сетки (тоньше)
    'grid_opacity_ratio': 0.3,             # Прозрачность сетки
    'grid_vertical_offset': 50,            # Смещение сетки (меньше)
    'margins': {'text': 71, 'logo': 50},   # Отступы для текста и логотипа
    'font_size': 150,                       # Размер шрифта (больше)
    'line_spacing_ratio': 0.2,             # Межстрочный интервал
    'gradient_colors': CONFIG['gradient_colors'],  # Те же цвета градиентов
    'image_alignment': 'bottom',           # Выравнивание по нижнему краю
    'max_lines': 3,                        # Максимальное количество строк
    'logo_size': (180, 180)                # Размер логотипа (180x180)
}
```

**Особенности градиентов:**
- **Нижний градиент**: Плотный цвет #46549A внизу, плавно переходит в #2A3050 и затухает кверху
- **Треугольник**: Градиент от #2A3050 (сверху) к #46549A (снизу)

**Особенности сетки:**
- **Размер квадрата**: 295x295 пикселей (квадрат), 200x200 пикселей (ландшафт)
- **Толщина линий**: 4 пикселя (квадрат), 3 пикселя (ландшафт)
- **Цвет**: Белый (#FFFFFF)
- **Прозрачность**: Адаптируется к градиенту (30% от прозрачности градиента по умолчанию)
- **Расположение**: Только в области градиента, выровнено по центру, смещено вниз

**Особенности выравнивания:**
- **Квадрат**: Центрирование изображения
- **Ландшафт**: Выравнивание по нижнему краю

**Особенности текста:**
- **Квадрат**: Без ограничений по строкам
- **Ландшафт**: Максимум 3 строки, при превышении добавляется многоточие (...)

**Особенности логотипа:**
- **Квадрат**: Оригинальный размер
- **Ландшафт**: Изменяется до 180x180 пикселей

## 📊 Логирование

Скрипт создает подробные логи в файле `image_processor.log` и выводит информацию в консоль:

- Время выполнения
- Количество обработанных изображений
- Ошибки и предупреждения
- Статистика обработки

## 🛠️ Обработка ошибок

Скрипт включает надежную обработку ошибок:

- Проверка существования папок и файлов
- Валидация введенного текста
- Graceful fallback при ошибках загрузки шрифтов
- Продолжение работы при ошибках отдельных изображений
- Подробные сообщения об ошибках

## 🔧 Производительность

- Использование numpy для быстрого создания градиентов
- Оптимизированные алгоритмы сжатия изображений
- Fallback методы при ошибках оптимизации
- Эффективное управление памятью

## 📝 Примеры использования

### Базовое использование
```bash
python main.py
# Введите текст: "Ваш текст здесь"
```

### Обработка большого количества изображений
Скрипт автоматически обрабатывает все изображения в папке `img/` и показывает прогресс.

## 🐛 Устранение неполадок

### Ошибка "Логотип не найден"
- Убедитесь, что в папке `logo/` есть файлы
- Проверьте права доступа к папке

### Ошибка загрузки шрифта
- Скрипт автоматически использует стандартный шрифт
- Проверьте наличие файла ACTAY-BOLD.OTF в папке `fonts/`

### Медленная обработка
- Убедитесь, что установлен numpy
- Проверьте размер исходных изображений

## 📈 Планы развития

- [ ] Поддержка различных форматов выходных файлов
- [ ] Пакетная обработка с конфигурационными файлами
- [ ] GUI интерфейс
- [ ] Поддержка анимаций и видео
- [ ] Интеграция с облачными сервисами

## 🤝 Вклад в проект

Если у вас есть предложения по улучшению или вы нашли ошибки, создайте issue или pull request.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. #   m y - b o t  
 